export const dashboardHtml = `<!doctype html><html lang="ru"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Открытая платформа камер</title><style>body{font:16px system-ui;background:#101827;color:#e6eef9;max-width:900px;margin:40px auto;padding:0 20px}form,li,.panel{background:#192438;padding:16px;border-radius:12px;margin:12px 0}input,select,button{padding:10px;margin:5px;border-radius:8px;border:1px solid #4b6284;background:#0e1625;color:#fff}button{background:#39b990;border:0;cursor:pointer}</style><h1>Открытая платформа камер</h1><p>Добавляйте только камеры из своей локальной сети. Пароли камеры в эту форму не передаются.</p><div class="panel"><button id="discover">Найти ONVIF-камеры в локальной сети</button><p id="found"></p></div><form id="camera"><input name="name" placeholder="Название камеры" required><select name="mode"><option value="onvif">ONVIF</option><option value="rtsp">RTSP</option></select><input name="address" placeholder="rtsp://192.168.1.30/live" required><button>Добавить камеру</button></form><p id="error"></p><ul id="list"></ul><script>const f=document.querySelector('#camera'),l=document.querySelector('#list'),e=document.querySelector('#error'),d=document.querySelector('#discover'),found=document.querySelector('#found');async function load(){const r=await fetch('/api/cameras');l.innerHTML=(await r.json()).map(c=>'<li><b>'+c.name+'</b><br>'+c.mode+' · '+c.address+'</li>').join('')}d.onclick=async()=>{d.disabled=true;found.textContent='Идёт поиск в локальной сети…';try{const r=await fetch('/api/discovery',{method:'POST'}),data=await r.json();found.textContent=data.addresses?.length?'Найдены службы: '+data.addresses.join(', '):'Совместимые ONVIF-камеры не найдены.'}finally{d.disabled=false}};f.onsubmit=async x=>{x.preventDefault();e.textContent='';const data=Object.fromEntries(new FormData(f));const r=await fetch('/api/cameras',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(data)});if(!r.ok)e.textContent=(await r.json()).error;else{f.reset();load()}};load()</script></html>`;
